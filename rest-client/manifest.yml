---
applications:
- name: bootiful-rest-client
  memory: 512M
  instances: 1
  host: bootiful-rest-client-${random-word}
  path: target/bootiful-rest-client.jar
  env:
    SPRING_PROFILES_ACTIVE: cloud
    DEBUG: "true"
    debug: "true"
